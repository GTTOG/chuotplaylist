# Backend Server Configuration
PORT=3000
NODE_ENV=development

# Frontend API Configuration (auto-detected on Vercel, only needed for custom domains)
# Leave empty - will auto-detect from window.location
REACT_APP_API_URL=

# Vercel Deployment
# These are automatically set by Vercel
# VERCEL_URL=your-project.vercel.app
# VERCEL_ENV=production
